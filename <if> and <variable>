#!/bin/bash - пишу какой интерпретатор использовать
RESULT=$(ls /etc) - так обьявляется переменная (знак $ обязателен!). Команда ls /etc возвращает список всех подфайлов
if [[ $RESULT == *"apt"* && $? == 0 ]]; условие if. * - произвольное количиство символов; $? - код ошибки (0 значит что ошибки нет)
then echo "SUCCESS" - вывод на экран SUCCESS
else echo "FAIL" вывод на экран FAIL
fi

#Очень важны пробелы, все они должны быть строго на своих местах!


